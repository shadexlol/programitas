#include <stdio.h>
#include <math.h>

/* Prototipo de la funcion esprimo() */
int esprimo (int);

int main()
{
	int x; /* esta variable irá valiendo 3,5,7,...,99 */
	
	printf ("2\n");  /* Imprimimos el 2, que es el primer numero primo, y el
	                    unico que es par */
						
	/* El resto de numeros son impares, desde 3 hasta 99, y para cada uno de
	   ellos habrá que comprobar si es primo o no. Si lo es, se imprime. */
	   
	   
	   
	   
	return 0;
}

/* Para comprobar si un numero n es primo, usamos esta función que hay que
   escribir. */
int esptimo (int n)
{
	/* Escribe aquí la función... */
	
	
	
	
	
	
	
}

