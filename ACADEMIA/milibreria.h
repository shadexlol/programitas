#define PI 3.1416
float area (float a)
		 	{
	 		float res1;
	 		res1=4*PI*pow(a,2);
	 		return res1;  //devolvemos nuestro resultado
	 	
		 }
float volumen (float b)
	 {
	 	float res1;
	 	res1=pow(b,3)*PI*4/3;
	 	return res1;
	 }
