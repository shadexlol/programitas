#include <math.h>

int main()
{
	float a,b;
	float mi_resultado;
	float tu_resultado;
	
	// Da valores a las variables "a" y "b"
	a =
	b =
	
	// Completa esta línea usando exp() y log() para calcular la potencia
	tu_resultado =
	
	// Yo calculo ahora esa misma potencia para que compares el resultado
	mi_resultado = pow(a,b);

	return 0;
}
