#include <math.h>
#define PI 3.141592654  // PI es una constante que podemos usar

int main()
{
	float a;
	float res;
	
	// Da un valor entre 0 y 2*PI a la variable "a"
	a = 
	res = sin(a)*sin(a) + cos(a)*cos(a);
	
	return 0;
}
