#include <math.h>

// Pon a partir de aquí el prototipo de la función potencia()

int main()
{
	float a,b;
	float mi_resultado;
	float tu_resultado;
	
	// Da valores a las variables "a" y "b"
	a =
	b =
	
	// Completa esta línea usando tu función potencia()
	tu_resultado =
	
	// Yo calculo ahora esa misma potencia para que compares el resultado
	mi_resultado = pow(a,b);

	return 0;
}

// Pon a partir de aquí la implementación de la función potencia

