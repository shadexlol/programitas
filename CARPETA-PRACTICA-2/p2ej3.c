#include <math.h>

int main()
{
	float x,b,c,n;
	float res;
	
	// Prueba el programa con los valores 4,8,16,256,65536 y 1048576
	b = 2;
	x = 256;
	
	printf("el log en base b con un numero x es: %f \n\n",log(x));

	// Completa esta línea para que calcule el logaritmo en base B de X
	
	printf("el logaritmo asi wapo de base es: %f \n\n", log(x)/log(b));
	
	return 0;
}
